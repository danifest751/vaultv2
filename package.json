{
  "name": "family-media-vault",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "bench:media-search": "npm run build && npm --workspace @family-media-vault/server run bench:media-search",
    "test:perf:media-search": "npm run build && npm --workspace @family-media-vault/server run bench:media-search:gate",
    "web:dev": "npm --workspace @family-media-vault/web run dev",
    "web:build": "npm --workspace @family-media-vault/web run build"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
